import Modal from '../src/modal.js'

new Modal({
	idModal: '#Modal',
	// shadow: false,
	// closeOnEscKey: false,
	// trapFocus: false,
	// firstFocus: false,
	// closeModalClick: false,
})

new Modal({
	idModal: '#Modal2',
	firstFocus: false,
	shadow: false,
})

new Modal({
	idModal: '#Modal3',
})
